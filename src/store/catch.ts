// import { API_CONTROLLER } from '../api/controller';
// import { onRunError } from '../app/redux/actions';
// import { store } from './index';
//
// API_CONTROLLER.controller.interceptors.response.use((response) => response, (error) => {
//     store.dispatch(onRunError(error));
// });


